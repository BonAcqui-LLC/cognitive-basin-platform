# Next Action

Prioritize UI and conformance polish rather than new risk.

## Recommended next tranche

1. improve the 720x1600 layout so the main activation and verification controls fit more comfortably on first view
2. add automated fixture comparison between `tests/golden-traces` and a selected export from `cognitive-basin-platform`
3. decide whether to populate `export_size_bytes` and `persistence_size_bytes` directly inside runtime SERA metrics

## Do later, with operator timing

- airplane-mode acceptance rerun
- full reboot persistence rerun
- microphone boundary implementation
- any custom-ROM research tied specifically to `XT2317-2` / `gnevan`
