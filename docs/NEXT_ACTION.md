# Next Action

Active branch:
`build/ephux-product-surface-codex`

Immediate tranche:
- deepen the local EphUX surface with richer evidence-review semantics and commit-approval workflow detail
- extend browser verification into report opening, hostile HTML checks, and keyboard-flow coverage
- begin provider-fabric and experiment ledger work on top of the new persisted product surface
- keep production infrastructure unchanged unless explicit human approval is given

Do not repeat baseline closure unless a regression appears.
